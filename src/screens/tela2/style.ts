import { StyleSheet } from "react-native"

export const Pg2style = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 20,
      },
      image: {
        width: 150,
        height: 150,
        margin: 20,
        marginTop: 30,
        borderRadius: 8,
      },
    });